# railway.toml — deploys both API and Web on Railway (Frankfurt region)

[build]
builder = "nixpacks"

[[services]]
name = "api"
source = "apps/api"

[services.env]
PORT = "3001"
NODE_ENV = "production"

[[services]]
name = "web"
source = "apps/web"

[services.env]
NODE_ENV = "production"
